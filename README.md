# Nolane Browser Agent

Experimental verification repository for the Nolane Browser Agent Chrome extension.

The first verification branch is used to reproduce and test the exact `38.0.6-ultra-hardened` release artifact across GitHub-hosted runners before promoting source into the default branch.
